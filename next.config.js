/** @type {import('next').NextConfig} */
const nextConfig = {
  // enlève l'objet `experimental` ou au minimum la clé `appDir`
  // garde ici le reste de ta config si tu as d'autres clés
};

module.exports = nextConfig;
